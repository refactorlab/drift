# grafana/tempo #6754 — v3.0: Add new architecture doc section

**[View PR on GitHub](https://github.com/grafana/tempo/pull/6754)**

| | |
|---|---|
| **Author** | @mapno |
| **Status** | Merged |
| **Source** | GitHub conversation page (fetched via web, no API token) |

## Top review comments (most substantive, verbatim)

### @Copilot
> The related resources link points to `configuration/#block`, but the block-builder section in the configuration reference is `#block-builder`.

### @Copilot
> The front matter uses a `redirect:` key, but this repo's docs front matter appears to standardize on `aliases:` for redirects

### @Copilot
> Elsewhere in this repo, pages commonly include a relative alias in addition to the `/latest/` alias so redirects work in both contexts.

### @knylander-grafana
> Good updates to the docs.

### @knylander-grafana
> Coordinated linting follow-up work in a separate PR (#6898) containing 12 minor Vale style guide corrections across the documentation files.

---
*Collected via parallel web fetch of the public GitHub PR page (no token, no API rate-limit budget used).*
