# plotly/plotly.py #4706 — Updates for maplibre maps

**[View PR on GitHub](https://github.com/plotly/plotly.py/pull/4706)**

| | |
|---|---|
| **Author** | @LiamConnors |
| **Status** | Merged |
| **Source** | GitHub conversation page (fetched via web, no API token) |

## Top review comments (most substantive, verbatim)

### @archmoj
> IMHO, for a while we need to keep mapbox examples until they could use the next plotly.py version in their systems.

### @ndrezn
> We should keep some mapbox examples, but like in `plotly.py` I would expect a deprecation notice associated with them.

### @archmoj
> There is also `hexbin_mapbox` in `figure_factory`...We should possibly add `hexbin_map`. No?

### @gvwilson
> I'm OK with shipping most of the maps in this release and doing another with the small number of maps we didn't get to.

### @archmoj
> I have one remaining comment...Other than that it looks great and works great too!

---
*Collected via parallel web fetch of the public GitHub PR page (no token, no API rate-limit budget used).*
