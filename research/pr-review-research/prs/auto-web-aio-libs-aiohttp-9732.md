# aio-libs/aiohttp #9732 — Add Client Middleware Support

**[View PR on GitHub](https://github.com/aio-libs/aiohttp/pull/9732)**

| | |
|---|---|
| **Author** | @bdraco |
| **Status** | Merged |
| **Source** | GitHub conversation page (fetched via web, no API token) |

## Top review comments (most substantive, verbatim)

> Note: Most of the inline review threads on this PR (~15 conversations by @Dreamsorcerer, later marked "resolved") did not render their prose in the web-fetched HTML page. The reviewer names and the web-retrievable prose are captured below.

### @Dreamsorcerer
> Also #6915. There are some examples in there of different use cases which should work when this is complete.

### @bdraco
> Thanks for the review. I'll clean this up between sprint work.

### @bdraco
> Since we're generally happy with the design now (as discussed on Matrix), I'll go ahead and merge this to unblock the follow-up work.

### @Dreamsorcerer (inline review threads — text not web-retrievable)
> Reviewed the implementation across `aiohttp/client.py`, `aiohttp/client_middlewares.py`, and `tests/test_client_middleware.py`, leaving multiple conversation threads that were later resolved. The verbatim prose of these inline comments did not render on the web conversation page.

---
*Collected via parallel web fetch of the public GitHub PR page (no token, no API rate-limit budget used).*
