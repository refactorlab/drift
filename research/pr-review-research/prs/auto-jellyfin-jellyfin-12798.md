# jellyfin/jellyfin #12798 — Refactor library.db into jellyfin.db and EFCore

**[View PR on GitHub](https://github.com/jellyfin/jellyfin/pull/12798)**

| | |
|---|---|
| **Author** | @JPVenson |
| **Status** | ✅ merged |
| **Opened** | 2024-10-09 |
| **Repo importance** | ★52,933 · 4,927 forks · score 77,636 |
| **Diff** | +20160 / −7581 across 160 files |
| **Engagement** | 18 conversation · 186 inline review comments |

## Top review comments (ranked by reactions)

### @JPVenson — 4 reactions  
`👍 1 · 🎉 1 · 🚀 2`  ·  [link](https://github.com/jellyfin/jellyfin/pull/12798#issuecomment-2407390184)

> >     * I assume yes, but all the changes to the previous repositories/implementations (at Emby.server.* namespace) are just there for having a serialization layer with the new entities for the migration and API, but all the new implementations is in the new Jellyfin namespace, right?
> 
> The old entities persist as the 2nd layer so i dont have to rewrite the _whole_ application. The new repositories map the new Entities to the old Dto's.
> 
> > 
> >     * Why some Guid don't have the `[DatabaseGenerated(DatabaseGeneratedOption.Identity)]` annotation but others do?
> 
> Depends. Some entities are generated in code as is (BaseItem) some can be generated by the Db for ease of use.
> 
> > 
> >     * What if we create an abstract class entity `BaseEntity` that has the PK field already included and a versioning behaviour, that it can be extended by others so we avoid some extra duplication? Like [this](https://github.com/jellyfin/jellyfin/blob/master/Jellyfin.Data/Entities/Permission.cs?rgh-link-date=2024-10-11T12%3A26%3A38Z#L59-L66)
> 
> Currently the structure of the BaseItem in relation the the wicked serialisation system is not up for refactoring. That will be a PR on its own with likely similar size.
> 
> > 
> > 
> > We should also benchmark at some point (although probably not for this PR, but something that's better sooner than later) if using ids vs guid pose a performance improvement or not (in the past we assumed yes, but perhaps we're doing premature optimization [1](https://www.mssqltips.com/sqlservertip/5105/sql-server-performance-comparison-int-versus-guid/) [2](https://stackoverflow.com/a/82936 … *[truncated]*

### @JPVenson — 1 reactions  
`👍 1`  ·  [link](https://github.com/jellyfin/jellyfin/pull/12798#issuecomment-2408658293)

> No worries. Short said, we dont have that issue as this only happens after you materialised values via SaveChanges and then keep the connection open which will introduce a WriteLock on the database.
> 
> This is not really a thing on our side, we follow a strict UnitOfWork pattern where after we saved changes we "throw away" the context cleaning up the connection.

### @JPVenson — 0 reactions  
`—`  ·  [link](https://github.com/jellyfin/jellyfin/pull/12798#issuecomment-2405840520)

> > Should store all of the `typeof(T).FullName` as consts in BaseItemRepository so we can reduce string allocation in each query
> 
> <s>Shouldn't dotnet store those anyway in its global string table as its compile time static?</s>
> 
> Seems like not?https://github.com/dotnet/runtime/issues/81564

### @darthShadow — 0 reactions  
`—`  ·  [link](https://github.com/jellyfin/jellyfin/pull/12798#issuecomment-2408490702)

> Just confirming my understanding, but I see that the separate RO/RW pools are missing at the moment and so are the various PRAGMAs that were being set. I guess those are minor details and targeted for a future PR once the bulk of the changes are merged?

### @JPVenson — 0 reactions  
`—`  ·  [link](https://github.com/jellyfin/jellyfin/pull/12798#issuecomment-2408615772)

> @darthShadow darthShadow
> > Just confirming my understanding, but I see that the separate RO/RW pools are missing at the moment
> 
> Pooling is done by EfCore internally now instead of our own implementations.
> 
> > and so are the various PRAGMAs that were being set. 
> 
> The only one i could find is a `pragma shrink_memory` that is executed on migration only. Most stuff was moved to a seperate migration. What else do you mean?

### @darthShadow — 0 reactions  
`—`  ·  [link](https://github.com/jellyfin/jellyfin/pull/12798#issuecomment-2408627908)

> > Pooling is done by EfCore internally now instead of our own implementations.
> 
> This was the case for 10.9 too, just using the Microsoft.Data.Sqlite package instead of EFCore fully, I believe. It also had the benefit of using 2 separate pools for RW & RO connections because SQLite allows only 1 writer but multiple readers, which allows massive scale with separate pools for each.
> 
> > What else do you mean?
> 
> There are a bunch more in the BaseSqliteRepository.cs file.
> 
> Reference: https://github.com/jellyfin/jellyfin/blob/822d4075843cdd4634faf738c2fb69b6b3201d5a/Emby.Server.Implementations/Data/BaseSqliteRepository.cs#L93-L176
> 
> The `_writeLock` was controlling the single write connection and a separate pool for read only connections opened in the read-only mode. There are also a bunch of PRAGMAs for improving the performance there.


---
*Collected automatically by `collect.ts` (no token, rate-limit-aware). Reaction counts are a snapshot at collection time.*
