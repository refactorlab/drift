# apache/iceberg #9695 — Add Scan Planning Endpoints to open api spec

**[View PR on GitHub](https://github.com/apache/iceberg/pull/9695)**

| | |
|---|---|
| **Author** | @rahil-c |
| **Status** | Merged (September 10, 2024) |
| **Source** | GitHub conversation page (fetched via web, no API token) |

## Top review comments (most substantive, verbatim)

### @dramaticlly
> looks like `FetchScanTasksRequest` only took opaque `plan-task` of string type and generated by the rest server, can we do get instead of post?

### @flyrain
> Oh i saw the comment in the design doc to keep it for 24 hours, which is reasonable to me. Can we provide this kind of recommendation in the spec?

### @flyrain
> Why do clients need to choose if the snapshot schema should be used in case of time traveling? Should this behavior be decided by the server?

### @danielcweeks
> there are two reasons: 1. We want the client to be explicit about which snapshot they want to read 2. This simplifies the request because there's only one way to read historical data

### @flyrain
> To help clarify, could we add a link here for further reading, or provide a detailed explanation?

### @jackye1995
> mostly looks good to me, just a few small comments. Thanks for keep pushing for this!

---
*Collected via parallel web fetch of the public GitHub PR page (no token, no API rate-limit budget used).*
